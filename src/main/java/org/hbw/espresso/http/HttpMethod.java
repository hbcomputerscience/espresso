package org.hbw.espresso.http;

/**
 * Enum containing all HTTP/1.1 methods. HttpMethod.ACTION will match all Methods.
 */
public enum HttpMethod {
	GET, HEAD, POST, PUT, DELETE, TRACE, OPTIONS, CONNECT, PATCH, ACTION
}
